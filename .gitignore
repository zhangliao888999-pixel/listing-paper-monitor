__pycache__/
*.pyc
*.lock

# 2026-07-29晚间新增: crash_watch.py每60秒把完整/trades响应整段追加写入这些文件,
# 跑得久的币(比如tnos2跑了好几个小时)会无限膨胀,tnos2_trades_archive.jsonl已经
# 涨到125MB,超过GitHub单文件100MB硬限制导致整个push被拒绝。这些文件只是本地取证
# 用的原始存档,docs/journal.html和docs/index.html都不读取它们,不需要进GitHub。
watch_bot/*_trades_archive.jsonl

# 2026-07-30新增: 实盘钱包私钥只能留在VPS本地,绝不能进git历史(哪怕仓库是
# 私有的也不行,私钥一旦进过commit历史就等于泄露,删掉文件不等于删掉历史记录)。
# vps_run_forever.ps1会从这个文件读私钥设成环境变量给snipe_exit.py用,用户
# 自己在VPS上创建这个文件、自己填内容,我不会经手实际的私钥值。
watch_bot/.live_wallet_key

# 2026-07-31新增: 实盘持仓标记目录,每个在跑的真实仓位一个以mint命名的文件。
# 纯运行时状态(建仓创建/平仓删除),VPS本地的事实,不该跟着代码同步。
watch_bot/.live_positions/
watch_bot/.live_position_open

# 2026-07-31新增: VPS运行时日志(supervisor/stdout/stderr)不再进git。
# 这些文件被正在跑的进程持续写入,Windows下git更新它们时反复报
# "unable to unlink old ...: Invalid argument",本会话已经因此卡住过多次pull,
# 每次都要停任务/杀进程才能继续。它们是VPS本地的运行时输出,不是需要跨机器
# 同步的数据(真正的数据是journal.jsonl/pump_lifecycle.json),不该被跟踪。
watch_bot/vps_stdout_*.log
watch_bot/vps_stderr_*.log
watch_bot/vps_supervisor_*.log
